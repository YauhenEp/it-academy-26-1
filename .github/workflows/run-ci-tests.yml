name: Run unit tests

on:
  push:
    branches: [main]
  pull_request:
    branches: 
      - "**"
  
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install dependencies
      working-directory: ./jestjs
      run: npm install

    - name: Run tests
      working-directory: ./jestjs
      run: npm run test